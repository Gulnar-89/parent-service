spring:
  application:
    name: eureka-server
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka-server/
  instance:
    hostname: localhost
server:
  port: 8761

# http://${eureka.instance.hostname}:${server.port}/eureka-server/